node_modules/
.pnpm-store/
# dist/ 자체는 ignore 하되, dist/types/ 만 추적해서 frontend 가 git URL 의존성
# 으로 pull 할 때 prepare 단계 (build:types) 없이도 .d.ts 가 즉시 resolve 되도록.
dist/*
!dist/types/
build/

.env
.env.*
!.env.example

*.log
.DS_Store

.vscode/
.idea/

coverage/
.tsbuildinfo
backtest-reports/
reports/
